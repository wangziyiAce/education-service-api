"""智能报告 V2 服务包。

这个包把原先“一个 report_service.py 里什么都做”的 MVP 写法拆开。
拆开的目的不是为了显得复杂，而是为了让项目具备企业项目常见的边界：

1. ``rules``：只放确定性计算，例如风险分、ROI、SLA 是否超时。
   这些数字必须由代码和 SQL 生成，不能交给大模型自由发挥。
2. ``schemas``：定义每一种报告自己的结构，前端和 Dify 都按这个契约对齐。
3. ``registry``：登记报告类型与聚合器、模板、权限、默认周期的关系。
4. ``aggregators``：面向数据库取数，把业务事实聚合成可解释的指标快照。
5. ``orchestrator``：编排任务状态、幂等、重试、AI 调用与 HTML 渲染。

你后面面试讲项目时，可以把这里概括为：
“我把智能报告模块从单文件 MVP 重构成注册表驱动的报告编排系统，
保证数字可追溯、报告类型可扩展、AI 只做解释不编造指标。”
"""

