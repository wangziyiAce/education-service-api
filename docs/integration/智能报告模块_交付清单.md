# 智能报告模块交付清单

## 冻结基线

- 权威分支：`master`
- 冻结范围：Iteration 3 及此前能力
- 延后范围：Iteration 4、阶段 4 生产封装、阶段 5 上线
- 技术边界：智能报告纯 Python；仅客服 Agent 使用 Dify + Python

## 必须随模块交付

- 后端：报告 Router、Schema、Model、`services/reporting/`、模板和调度入口。
- 前端：报告 API、TypeScript 类型、报告页面、报告助手组件、Router、菜单和 API Catalog 登记。
- 数据库：报告 Migration、演示 Seed 及对应 Cleanup。
- 契约：`openapi-iteration3.json`、契约测试和《智能报告模块_集成联调说明》。
- 配置：`.env.example` 中的报告占位变量；真实 `.env` 和密钥不得进入 Git。

## 合并验收门

- [ ] `master` 与 Berlin 指向相同提交，工作区干净。
- [ ] OpenAPI 快照由真实 `main.app` 生成并与运行时一致。
- [ ] 冻结报告路径全部存在，operationId 全局唯一。
- [ ] 前端 API Catalog 覆盖冻结报告路径。
- [ ] 报告专项契约测试通过。
- [ ] 真实 MySQL、DeepSeek、异步任务、详情轮询和助手追问通过。
- [ ] Evidence、DataQuality、周期比较和跨报告关系可在前端展示。
- [ ] `.env` 未跟踪，代码与文档不存在真实密钥。

真实外部服务验收属于下一阶段；本清单勾选前必须附带实际命令或请求结果，不能用历史测试结果代替。
