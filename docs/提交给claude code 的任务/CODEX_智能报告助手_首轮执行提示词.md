# Codex 任务：智能报告助手首轮审计

你是一名资深 AI 应用开发工程师和 Python 后端架构师。

请读取并遵守：

`doc/智能报告助手_智能交互与决策层迭代计划.md`

核心原则：不推翻现有架构，只在 `Registry → Aggregator → Rules → Schema → Orchestrator → Renderer` 之上增加智能交互与决策层。业务数字仍由 SQL、Aggregator 和 Rules 计算；LLM 不访问数据库、不写 SQL、不直接写库。

## 本轮仅执行

`Iteration 0：代码审计与契约冻结`

## 必须完成

1. 阅读根目录 `CLAUDE.md`。
2. 阅读智能报告相关后端、前端和测试。
3. 运行现有报告基线测试。
4. 创建 `doc/智能报告助手_代码审计与契约基线.md`。
5. 文档必须包含：
   - CLAUDE.md 相关规则；
   - 当前调用链；
   - 可复用函数和真实签名；
   - 十类报告 Registry 定义；
   - 权限、幂等、数据质量和追溯机制；
   - 智能层目录建议；
   - 文件级影响；
   - 公共文件修改判断；
   - 计划与真实代码差异；
   - Iteration 1 修订步骤；
   - 测试结果；
   - 风险和待确认问题。

## 本轮禁止

- 修改业务代码；
- 新增依赖；
- 数据库迁移；
- 删除 Dify；
- 修改前端；
- 修改公共文件；
- 开始 Iteration 1。

若计划与 `CLAUDE.md` 冲突，以 `CLAUDE.md` 为准并记录差异。完成审计文档后停止，等待人工确认。
