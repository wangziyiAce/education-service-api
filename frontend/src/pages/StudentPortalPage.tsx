import { ArrowRight, FileClock, GraduationCap, MessageSquare } from 'lucide-react'
import { Link, useSearchParams } from 'react-router-dom'
import PageHeader from '@/components/shared/PageHeader'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { useAuthStore } from '@/stores/auth-store'

export default function StudentPortalPage() { const user = useAuthStore((s) => s.user); const [params] = useSearchParams(); const preview = params.get('preview') === 'student' && user?.role_code !== 'student'; return <div><PageHeader title={preview ? '学生门户预览' : `你好，${user?.real_name || user?.username || '同学'}`} description={preview ? '只切换前端视图，所有请求仍使用管理员本人权限。' : '从这里进入咨询、课程与活动服务。'} /><div className="grid gap-4 md:grid-cols-2"><Link to="/customer-service"><Card className="h-full transition-shadow hover:shadow-md"><CardHeader><CardTitle className="flex gap-2"><MessageSquare />客服咨询</CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground">保存个人会话，查看课程和活动并完成报名。</p><span className="mt-4 flex items-center gap-2 text-sm text-primary">进入咨询 <ArrowRight className="h-4 w-4" /></span></CardContent></Card></Link><Card><CardHeader><CardTitle className="flex gap-2"><GraduationCap />学生服务</CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground">当前只展示后端已开放的真实能力。</p><Badge className="mt-4" variant="secondary">真实数据边界</Badge></CardContent></Card></div><h2 className="mt-8 text-lg font-semibold">尚未开放</h2><div className="mt-3 grid gap-4 md:grid-cols-3">{['申请与签证进度','成绩与课程记录','关怀与人工介入'].map((x) => <Card key={x}><CardContent className="pt-6"><FileClock className="h-5 w-5 text-muted-foreground" /><p className="mt-3 font-medium">{x}</p><p className="mt-1 text-xs text-muted-foreground">后端接口尚未开放，不展示模拟数据。</p></CardContent></Card>)}</div></div> }
